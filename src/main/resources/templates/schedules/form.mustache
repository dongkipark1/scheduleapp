<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>{{#schedule.id}}일정 수정{{/schedule.id}}{{^schedule.id}}새 일정 등록{{/schedule.id}}</title>
</head>
<body>

{{#schedule}}
    <h1>
        {{#id}}✏️ 일정 수정{{/id}}
        {{^id}}➕ 새 일정 등록{{/id}}
    </h1>

    {{#globalErrors}}
        <div style="color: red; margin-bottom: 1rem">{{.}}</div>
    {{/globalErrors}}

    <form action="/schedules" method="post">
        {{#id}}
            <input type="hidden" name="id" value="{{id}}">
        {{/id}}

        <label>{{msg.title}}</label><br>
        <input type="text" name="title" value="{{title}}"><br>
        {{#fieldErrors.title}}
            <div style="color:red; font-size: 0.9em">{{.}}</div>
        {{/fieldErrors.title}}<br>

        <label>{{msg.description}}</label><br>
        <textarea name="description" rows="4" cols="50">{{description}}</textarea><br><br>

        <label>{{msg.dueDate}}</label><br>
        <input type="datetime-local" name="dueDate" value="{{dueDate}}" min="{{now}}" required><br>
        {{#fieldErrors.dueDate}}
            <div style="color:red; font-size: 0.9em">{{.}}</div>
        {{/fieldErrors.dueDate}}<br>

        <label>
            <input type="checkbox" name="completed" {{#completed}}checked{{/completed}}>
            {{msg.completed}}
        </label><br><br>

        <button type="submit">{{msg.save}}</button>
        <a href="/schedules">{{msg.back}}</a>
    </form>
{{/schedule}}

</body>
</html>
